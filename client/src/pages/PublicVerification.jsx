/** @file PublicVerification.jsx @description Public credential lookup entry screen. @layer Client Page @interacts Shared form controls and route navigation. @futureWork Send identifier to verification API. @nonGoal Do not validate credentials or implement QR scanning. */
import { useNavigate } from 'react-router-dom';
import { Button, Card, Input } from '../components';
export default function PublicVerification() { const navigate = useNavigate(); const submit = (event) => { event.preventDefault(); navigate('/verify/DEMO-2026-001'); }; return <div className="mx-auto grid min-h-[calc(100vh-4rem)] max-w-5xl place-items-center px-5 py-12"><div className="w-full max-w-2xl"><p className="text-center text-xs font-bold uppercase tracking-[.18em] text-cyan-700">Public verification</p><h1 className="mt-4 text-center text-3xl font-semibold tracking-tight md:text-4xl">Verify an academic credential</h1><p className="mx-auto mt-4 max-w-xl text-center leading-7 text-slate-600">Enter a credential ID to view its verification result. This prototype uses a demonstration result only.</p><Card className="mt-8"><form className="flex flex-col gap-3 sm:flex-row sm:items-end" onSubmit={submit}><Input id="credential-id" className="flex-1" label="Credential ID" placeholder="e.g. CV-2026-000184" hint="No QR scanning or live lookup is connected." /><Button type="submit" className="sm:mb-6">Verify</Button></form></Card></div></div>; }
