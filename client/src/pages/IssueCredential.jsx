/** @file IssueCredential.jsx @description Static credential-issuance form. @layer Client Page @interacts Shared inputs and modal. @futureWork Add client validation and API submission. @nonGoal Do not create credentials or sign data. */
import { useState } from 'react';
import { Button, Card, Input, Modal } from '../components';
export default function IssueCredential() { const [open, setOpen] = useState(false); return <div className="mx-auto max-w-3xl"><div><p className="text-sm font-medium text-slate-500">Institution workspace</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Issue a credential</h1><p className="mt-2 text-sm text-slate-600">Capture the academic record to be issued. This form does not save data yet.</p></div><Card className="mt-8"><form className="grid gap-5 md:grid-cols-2" onSubmit={(event) => { event.preventDefault(); setOpen(true); }}><Input id="student-name" label="Student name" placeholder="Full legal name" /><Input id="student-id" label="Student ID" placeholder="University ID" /><Input id="degree" label="Degree / award" placeholder="e.g. Bachelor of Arts" /><Input id="department" label="Department" placeholder="e.g. School of Humanities" /><Input id="graduation" label="Graduation date" type="date" /><Input id="issue-date" label="Issue date" type="date" /><div className="md:col-span-2 flex justify-end border-t border-slate-100 pt-5"><Button type="submit">Review credential</Button></div></form></Card><Modal open={open} title="Credential review" onClose={() => setOpen(false)}><p className="text-sm leading-6 text-slate-600">Issuance is intentionally unavailable in this frontend-only step. Backend validation, signing, and ledger recording will be added later.</p></Modal></div>; }
