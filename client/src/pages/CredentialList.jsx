/** @file CredentialList.jsx @description Static credential list shell. @layer Client Page @interacts Shared cards, badges and empty state. @futureWork Add search, pagination and backend data. @nonGoal Do not expose real credential records. */
import { Link } from 'react-router-dom';
import { Badge, Button, Card, EmptyState } from '../components';
export default function CredentialList() { return <div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-slate-500">Institution workspace</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Credentials</h1></div><Link to="/institution/credentials/new"><Button>Issue credential</Button></Link></div><Card className="mt-8" header={<div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Issued credentials</h2><p className="mt-1 text-sm text-slate-500">Static preview — no records are connected.</p></div><input aria-label="Search credentials" placeholder="Search placeholder" className="rounded-md border border-slate-300 px-3 py-2 text-sm" /></div>}><div className="hidden overflow-x-auto md:block"><table className="w-full text-left text-sm"><thead className="border-b border-slate-200 text-xs uppercase tracking-wide text-slate-500"><tr><th className="pb-3 font-medium">Recipient</th><th className="pb-3 font-medium">Credential</th><th className="pb-3 font-medium">Issued</th><th className="pb-3 font-medium">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{[['Aarav Mehta', 'Bachelor of Engineering', '12 Aug 2026', 'Active'], ['Lena Thomas', 'Master of Public Policy', '04 Aug 2026', 'Active'], ['Samira Khan', 'Bachelor of Science', '31 Jul 2026', 'Revoked']].map(([name, credential, date, status]) => <tr key={name}><td className="py-4 font-medium">{name}</td><td className="py-4 text-slate-600">{credential}</td><td className="py-4 text-slate-600">{date}</td><td className="py-4"><Badge tone={status === 'Active' ? 'success' : 'danger'}>{status}</Badge></td></tr>)}</tbody></table></div><div className="md:hidden"><EmptyState title="Credential list preview" description="The tabular placeholder is available on medium and larger screens." /></div></Card></div>; }
