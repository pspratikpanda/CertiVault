/** @file Input.jsx @description Labeled form input for static client forms. @layer Client UI @interacts Login, issuing and verification pages. @futureWork Add validation messaging driven by form state. @nonGoal Do not submit or validate data against a server. */
export default function Input({ label, hint, id, className = '', ...props }) { return <label className={`block ${className}`} htmlFor={id}><span className="mb-1.5 block text-sm font-medium text-slate-700">{label}</span><input id={id} className="block w-full rounded-md border border-slate-300 bg-white px-3 py-2.5 text-sm text-slate-950 outline-none placeholder:text-slate-400 focus:border-slate-700 focus:ring-2 focus:ring-slate-200" {...props} />{hint && <span className="mt-1.5 block text-xs text-slate-500">{hint}</span>}</label>; }
