/** @file Card.jsx @description Reusable bordered content surface. @layer Client UI @interacts All page-level presentation components. @futureWork Support optional density variants. @nonGoal Do not store page state. */
export default function Card({ children, className = '', header, footer }) { return <section className={`rounded-lg border border-slate-200 bg-white shadow-sm ${className}`}>{header && <div className="border-b border-slate-200 px-5 py-4">{header}</div>}<div className="p-5">{children}</div>{footer && <div className="border-t border-slate-200 px-5 py-4">{footer}</div>}</section>; }
